import fs from "fs-extra";

const buildPackageJson = async ({ package, buildVersion, buildFolder }) => {
    const { name }
    const packageJson = {
        version:buildVersion
    };
    // Get dependencies from root package
    const rootPackageJson = require()
};

export default buildPackageJson;
